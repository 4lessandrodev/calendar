import { IResult } from "../types";
import Result from "./result";
/**
 * @description Create an instance of Result as failure state.
 * @param error generic type E
 * @param metaData generic type M
 * @returns instance of Result as failure state
 *
 * @augments E generic type for error.
 * @default string.
 *
 * @argument M generic type for metaData.
 * @default Object empty object {}.
 *
 * @argument P generic type for payload.
 * @default void as no state.
 */
declare function Fail(): Result<string, string, {}>;
/**
 * @description Create an instance of Result as failure state.
 * @param error generic type E
 * @param metaData generic type M
 * @returns instance of Result as failure state
 *
 * @augments E generic type for error.
 * @default string.
 *
 * @argument M generic type for metaData.
 * @default Object empty object {}.
 *
 * @argument P generic type for payload.
 * @default void as no state.
 */
declare function Fail(): IResult<string, string, {}>;
/**
 * @description Create an instance of Result as failure state.
 * @param error generic type E
 * @param metaData generic type M
 * @returns instance of Result as failure state
 *
 * @augments E generic type for error.
 * @default string.
 *
 * @argument M generic type for metaData.
 * @default Object empty object {}.
 *
 * @argument P generic type for payload.
 * @default void as no state.
 */
declare function Fail<E, M extends {} = {}, P = void>(error: E extends void ? null : E, metaData?: M): Result<P, E extends void ? string : E, M>;
/**
 * @description Create an instance of Result as failure state.
 * @param error generic type E
 * @param metaData generic type M
 * @returns instance of Result as failure state
 *
 * @augments E generic type for error.
 * @default string.
 *
 * @argument M generic type for metaData.
 * @default Object empty object {}.
 *
 * @argument P generic type for payload.
 * @default void as no state.
 */
declare function Fail<E, M extends {} = {}, P = void>(error: E extends void ? null : E, metaData?: M): IResult<P, E extends void ? string : E, M>;
export default Fail;
export { Fail };
//# sourceMappingURL=fail.d.ts.map